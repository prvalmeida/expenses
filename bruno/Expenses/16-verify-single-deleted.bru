meta {
  name: Verify the deleted parcela is gone
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/expenses/{{firstExpenseId}}
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body.error.code: eq NOT_FOUND
}
